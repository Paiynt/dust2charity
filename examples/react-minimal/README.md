# Minimal React integration example

This example shows how an app or wallet UI could call the SDK functions.

Prereqs:
- You already have a Solana `connection`
- You already have a wallet adapter with `publicKey` + `sendTransaction`

See `DonateWidget.tsx`.
